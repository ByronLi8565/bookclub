node_modules

# vite build output
dist

# wrangler local dev state
.wrangler

# local-dev secret values (never commit)
.dev.vars

# agents dump
docs

# bombadil
.bombadil

# playwright
test-results
playwright-report
.playwright

# e2e (live-worker) run artifacts
e2e/runs

# local data-recovery backup (do not commit)
recovery/
