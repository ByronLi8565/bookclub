node_modules

# vite build output
dist

# wrangler local dev state
.wrangler

# local-dev secret values (never commit)
.dev.vars

# agents dump
docs

# bombadil
.bombadil

# playwright
test-results
playwright-report
.playwright

# e2e (live-worker) run artifacts
e2e/runs

# local data-recovery backup (do not commit)
recovery/

# capacitor native build artifacts (the ios/ and android/ project dirs
# themselves are committed once `cap add` scaffolds them; only their generated
# build output is ignored)
ios/App/Pods
ios/App/App/public
ios/App/build
ios/DerivedData
android/.gradle
android/app/build
android/build
android/app/src/main/assets/public
android/capacitor-cordova-android-plugins
